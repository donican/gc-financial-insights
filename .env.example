# universo de tickers
TICKERS=AAPL,MSFT,AMZN,GOOGL,META
# horizonte histórico (~3 anos)
HORIZON_DAYS=1100

# saída local (dev) OU GCS (prod)
# para local:
OUTPUT_URI=./data
# para GCS (com ADC configurado na máquina/host):
# OUTPUT_URI=gs://gc-financial-insights-dev-bucket

PRICES_PREFIX=bronze/prices
MANIFEST_PREFIX=_manifests

# caminho opcional para credencial SA se quiser GCS via chave (não obrigatório)
# GOOGLE_APPLICATION_CREDENTIALS=/secrets/sa.json
